description: >
  This command echos "Hello World" using file inclusion.
# What will this command do?
# Descriptions should be short, simple, and clear.
# parameters:
#   to:
#     type: string
#     default: "World"
#     description: "Hello to whom?"
steps:
  - run:
      # environment:
      #   PARAM_TO: <<parameters.to>>
      name: Hello Greeting
      command: curl -G "https://circleci.com/api/v2/workflow/$CIRCLE_WORKFLOW_ID" -H "circle-token: $CIRCLE_TOKEN" | jq -r '.pipeline_id'
